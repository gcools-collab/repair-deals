import { analyzeFinancials, type RepairEstimate } from "../deal-economics/index.ts";
import { createDealDecisionContext } from "../deal-decision-context/index.ts";
import { scanLeboncoin } from "../leboncoin-scanner.ts";
import { adaptMarketEstimateV2, estimateMarketV2 } from "../market-intelligence-v2/index.ts";
import { leboncoinMarketProviderV2 } from "../market-intelligence-v2/provider.ts";
import { executePartsSearchV2 } from "../parts-search-v2/index.ts";
import { configuredAutomaticPartProviders } from "../parts-intelligence/providers/configured.ts";
import type { PartSearchInput } from "../parts-intelligence/types.ts";
import { calculateOpportunityScore, type GlobalListingAnalyzer, type GlobalListingProvider } from "./index.ts";

export const globalLeboncoinProvider:GlobalListingProvider={async search(query,config){return(await scanLeboncoin({query,min_price:config.minPurchasePrice,max_price:config.maxPurchasePrice,limit:config.maxListingsPerQuery,broken_only:true,...(config.radiusKm?{radius_km:config.radiusKm}:{}),...(config.location.postalCode?{postal_code:config.location.postalCode}:{}),...(config.location.latitude!==null&&config.location.longitude!==null?{latitude:config.location.latitude,longitude:config.location.longitude}:{})})).results;}};
export const analyzeGlobalListing:GlobalListingAnalyzer=async(listing,config,budget)=>{const providers=configuredAutomaticPartProviders().map(entry=>entry.provider?{...entry,provider:{descriptor:entry.provider.descriptor,async search(input:PartSearchInput,queries:string[]){if(!budget.consume())throw new Error("request_budget_reached");return entry.provider!.search(input,queries);}}}:entry);const parts=await executePartsSearchV2({title:listing.title,description:listing.description,brand:listing.brand,model:listing.modelReference,attributes:listing.attributes,repairKeywords:listing.detectedFaultKeywords,detectedFaults:null,currency:"EUR"},providers);const selectedDiagnostic=parts.diagnostics[0]||null;const marketProvider={...leboncoinMarketProviderV2,async search(query:string,limit:number){if(!budget.consume())throw new Error("request_budget_reached");return leboncoinMarketProviderV2.search(query,limit);}};const marketV2=await estimateMarketV2({identity:parts.identity},marketProvider);const marketAdapted=adaptMarketEstimateV2(marketV2,config.minimumMarketConfidence);const ranked=parts.primaryResults.flatMap(r=>r.ranking.allRanked);const recommended=ranked.filter(r=>r.candidate.totalPrice!==null&&r.compatibilityScore>=80).sort((a,b)=>b.overallScore-a.overallScore)[0]||null;const selectedParts=recommended?[recommended.candidate]:[];const repairCost=recommended?.candidate.totalPrice??null;const partsConfidence=recommended?.overallScore??null;const risk=selectedDiagnostic?.hiddenRisk??null;const repairEstimate:RepairEstimate={probableParts:selectedParts.map(p=>({name:p.partName||"Pièce",reference:p.partReference,quantity:p.quantity||1,unitPriceLow:p.totalPrice,unitPriceHigh:p.totalPrice,source:p.provider?{kind:"provider",name:p.provider.name,url:p.url,retrievedAt:p.retrievedAt}:null})),partsCostLow:repairCost,partsCostHigh:repairCost,estimatedMinutesLow:null,estimatedMinutesHigh:null,difficulty:selectedDiagnostic?.repairDifficulty==="unknown"?null:selectedDiagnostic?.repairDifficulty||null,hiddenFaultRisk:risk,confidence:partsConfidence,source:recommended?.candidate.provider?{kind:"provider",name:recommended.candidate.provider.name}:null};const financial=analyzeFinancials({purchasePrice:listing.price,marketEstimate:marketAdapted.estimate,repairEstimate,extraCosts:0});const context=createDealDecisionContext({listing,resolvedIdentity:parts.identity,diagnostics:parts.diagnostics,selectedDiagnostic,partRequirements:parts.requirements,partSearchResults:parts.searchResults,selectedParts,marketEstimate:marketAdapted.estimate,repairEstimate,financialEstimate:financial,purchaseBenefits:[],warnings:[...parts.warnings,...marketV2.warnings]});const market=marketV2.weightedMedian??marketV2.medianPrice;const invested=listing.price!==null&&repairCost!==null?listing.price+repairCost:null;const margin=market!==null&&invested!==null?market-invested:null;const roi=margin!==null&&invested!>0?margin/invested!*100:null;const confidence=[parts.identity.confidence,marketV2.confidence,partsConfidence,financial.financialConfidence].filter((x):x is number=>x!==null).reduce((a,b,_,all)=>a+b/all.length,0)||null;const repairability=selectedDiagnostic?({easy:90,moderate:70,hard:45,expert:25,unknown:null}[selectedDiagnostic.repairDifficulty]):null;const availability=recommended?({in_stock:90,limited:60,preorder:35,out_of_stock:0,unknown:null}[recommended.candidate.availability||"unknown"]):null;const breakdown=calculateOpportunityScore({purchaseCost:listing.price,repairCost,marketValue:market,risk,globalConfidence:confidence,repairability,partsAvailability:availability,repairMinutes:null,liquidityScore:null,financial});const reasons=[] as import("./types.ts").OpportunityReason[];if(market===null)reasons.push("missing_market");if(!recommended)reasons.push("missing_parts");if(parts.identity.confidence<60)reasons.push("low_identity_confidence");if(marketV2.confidence<config.minimumMarketConfidence)reasons.push("low_market_confidence");if(partsConfidence===null||partsConfidence<config.minimumPartsConfidence)reasons.push("low_parts_confidence");if(financial.financialConfidence===null||financial.financialConfidence<config.minimumFinancialConfidence)reasons.push("low_financial_confidence");if(risk!==null&&risk>config.maxRisk)reasons.push("high_risk");if(margin!==null&&margin<0)reasons.push("negative_margin");else if(margin!==null&&margin<config.minMargin)reasons.push("below_margin");if(roi!==null&&roi<config.minRoi)reasons.push("below_roi");if(financial.readiness==="incomplete")reasons.push("incomplete_financials");const rejected=reasons.some(r=>["high_risk","negative_margin","below_margin","below_roi"].includes(r));const ready=!rejected&&reasons.length===0;return{listing,decisionContext:context,opportunityScore:ready?breakdown.overallOpportunityScore:null,opportunityBreakdown:breakdown,estimatedPurchaseCost:listing.price,estimatedRepairCost:repairCost,estimatedMarketValue:market,estimatedNetMargin:margin,roi,maxRecommendedPurchasePrice:financial.maxRecommendedPurchasePrice,risk,confidence,readiness:rejected?"rejected":ready?"ready":"partial",reasons,warnings:context.warnings,rankingBadges:[],liquidity:null};};
