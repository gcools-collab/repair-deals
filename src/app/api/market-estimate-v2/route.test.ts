import assert from "node:assert/strict"; import test from "node:test"; import { createMarketEstimateV2Handler } from "./route.ts";
test("route rebuilds identity and returns public V2 data",async()=>{const handler=createMarketEstimateV2Handler({id:"mock",name:"Mock market",async search(){return[];}});const response=await handler(new Request("http://localhost/api/market-estimate-v2",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({title:"MacBook Pro 13 2016 Touch Bar 256 Go A1706"})}));assert.equal(response.status,200);const body=await response.json();assert.equal(body.identity.family,"MacBook Pro");assert.equal(body.estimate.source,"Mock market");assert.ok(Array.isArray(body.estimate.tiersAttempted));assert.equal(JSON.stringify(body).includes("credential"),false);});
