import{DealHistoryService,type DealActualOutcome}from"@/lib/repair-intelligence-history";export const runtime="nodejs";const service=new DealHistoryService();
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){try{const{id}=await params;const body=await request.json() as Partial<DealActualOutcome>;if(!body||typeof body!=="object"||Array.isArray(body))throw new TypeError("Résultat invalide");return Response.json(await service.patchOutcome(id,body));}catch(error){if(error instanceof TypeError)return Response.json({error:{code:"validation_error",message:error.message}},{status:422});if(error instanceof Error&&error.message==="deal_not_found")return Response.json({error:{code:"not_found",message:"Deal introuvable"}},{status:404});return Response.json({error:{code:"outcome_failed",message:"Impossible de mettre à jour le résultat"}},{status:500});}}
